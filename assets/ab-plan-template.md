# AB Experiment Plan Template

飞书文档标题必须使用：`AB实验 | <实验名称>`。

正式文档不要写：`agent生成`、`AI生成`、`自动生成`、`实验概览`、`实验假设`、`唯一变量`、`分组字典`、指标`用途`、陌生读者自检过程、内部配置字段。

## 背景必要性

- 当前业务问题/机会：
- 实验为何需要 AB 验证：
- 关键证据：<来源链接或附件>。该来源证明：

## 评估指标

### 核心指标

- 标准名称：
- key：
- 映射状态：已映射标准库 / 需新增标准指标

### 过程指标

无。

### 护栏指标

- 标准名称：
- key：

指标可以写短定义；不要写用途。核心指标和护栏指标必须映射 AB 系统标准库并保留标准名称与 key。过程指标仅在标准库已有且确实帮助解释当前决策时填写。护栏只允许一项：收入、付费、价格、SKU、折扣、Paywall 类核心指标填留存率；留存、活跃、互动、召回类核心指标填收入 ARPU。投诉、退款、负反馈等写在风险说明或上线监控，不写在实验指标。

## 实验对象

只写谁有资格进入实验，不要重复场景条件。

## 实验场景

只写符合对象的用户在什么行为或位置实际受到策略影响；不要重复国家、VIP、历史行为等对象条件，也不要补造未确认入口。

## 需要新增的数据支持

无需新增数据支持。

如需新增，只写需要新增的事件、字段、看板或数据回传项。

## 互斥和灰度比例

### 互斥

无。

如有互斥，只写：与「<实验/活动名称>」互斥；最终选择的互斥后可用比例为 <比例>。若仍在样本量测算阶段、owner 尚未选择组合，写：`待根据所需有效流量选择互斥可用比例与正式灰度组合`。

### 灰度比例

- 正式灰度比例：

## 具体分组说明

| 组别 | 分流比例 | 用户实际看到的内容 |
| --- | --- | --- |
| 对照组 |  |  |
| 实验组 |  |  |

默认写清用户实际体验与核心差异。若简述不足以判断差异，补充关键内容、示例，或链接/附上原始配置、价格表等来源材料。

## 样本量设置

- 实验周期：
- MDE：
- MDE 依据：
- 真实目标用户规模：
- 所需有效流量：
- 互斥后可用比例：
- 正式灰度比例：
- 组间分流比例：
- 每组有效样本：
- 样本量结论：

## 上线前检查、实验过程中的复盘与提醒

-

## 来源与附件

逐条保留本次沟通中 owner 提供并用于支撑方案判断的原始资料；填写链接、附件或可访问路径，不按正文是否已引用筛选。

- <来源名称>：<链接 / 附件 / 路径>
